const styles = theme => ({
  root: {
    marginTop: 40,
    display: "flex",
    padding: `0 ${theme.spacing.unit * 3}px`
  },

  content: {
    width: 625,
    margin: "auto"
  }
});

export default styles;
